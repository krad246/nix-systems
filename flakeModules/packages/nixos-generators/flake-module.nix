_: {
  # FIXME(dendritic-hosts): Legacy Windex and Fortress generator outputs are
  # temporarily omitted because they force known-broken legacy NixOS and IFD
  # paths during CI. Restore them as sparse image declarations beneath
  # dendritic.configurations.*.hosts.
}
