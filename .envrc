#!/usr/bin/env direnv

use flake "$PWD" --accept-flake-config --inputs-from "$PWD"

watches=(
  $PWD/tools/*.nix
)

for watch in "${watches[@]}"; do
  readarray -t files < <(find "$watch" -type f -print)
  watch_file "${files[@]}"
done

source_env "$PWD/scripts"
